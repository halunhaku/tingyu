name: Flutter

on:
  push:
    paths:
      - 'app/**'
      - '.github/workflows/flutter.yml'
  pull_request:
    paths:
      - 'app/**'
      - '.github/workflows/flutter.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: app

concurrency:
  group: flutter-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: analyze + test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      # media_kit / audio_service_mpris / flutter_secure_storage 在 Linux 上需要这些原生依赖才能解析与构建
      - name: Install Linux native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libmpv-dev libsecret-1-dev libayatana-appindicator3-dev \
            ninja-build libgtk-3-dev pkg-config
      - run: flutter pub get
      # drift 的 database.g.dart 是提交进仓库的；schema 改了却忘记重新生成，
      # 会在运行时才发现。这里用"重新生成后必须无 diff"来兜底。
      - name: Verify generated drift code is up to date
        run: |
          dart run build_runner build
          git diff --exit-code --stat
      - run: flutter analyze
      - run: flutter test

  build:
    name: build (${{ matrix.os }})
    needs: analyze
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            target: macos
            # 只收产物目录：`**/Release/**` 会把 Build/Intermediates 里的同名目录一起收进来（近 1GB）
            artifacts: app/build/macos/Build/Products/Release/**
          - os: windows-2022
            target: windows
            artifacts: app/build/windows/x64/runner/Release/**
          - os: ubuntu-22.04
            target: linux
            # Flutter 的 Linux 产物是小写的 release/bundle —— 旧写法（大写 Release）在 Linux 上什么都匹配不到，
            # 于是 tingyu-linux 这个 artifact 一直没上传（构建其实是成功的）。
            artifacts: app/build/linux/x64/release/bundle/**
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Install Linux native dependencies
        if: matrix.target == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libmpv-dev libsecret-1-dev libayatana-appindicator3-dev \
            ninja-build libgtk-3-dev pkg-config
      - run: flutter pub get
      - name: Build
        run: flutter build ${{ matrix.target }} --release
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tingyu-${{ matrix.target }}
          path: ${{ matrix.artifacts }}
          if-no-files-found: warn
          retention-days: 7
